# App URL — used for OpenRouter referrer header (optional)
# Set this to your deployed Vercel URL
NEXT_PUBLIC_APP_URL=https://archivrr.vercel.app

# Note: OpenRouter API key is managed in the app's Settings page (stored in localStorage)
# No server-side API key is needed — each user provides their own key
